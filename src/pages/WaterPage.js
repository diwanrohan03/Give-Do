import React from "react";

const WaterPage = () => (
  <div style={{ textAlign: "center", padding: "2rem" }}>
    <h1>Clean Water Initiative</h1>
    <p>Ensure clean water reaches every community.</p>
  </div>
);

export default WaterPage;
